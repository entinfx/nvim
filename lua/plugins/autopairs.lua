-- Description
--
-- Automatically insert/delete brackets, paranthesis, curly braces, quotes.

return {
    'windwp/nvim-autopairs',
    event = "InsertEnter",
    config = true
    -- use opts = {} for passing setup options
    -- this is equivalent to setup({}) function
}

